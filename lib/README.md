# Library Layout

Step 2 reserves this folder for application modules such as `db`, `events`,
`sources`, `ingestion`, and `clustering`.

The folder exists now so later phases can add modules without reshaping the
repo.
